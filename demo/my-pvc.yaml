apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: myclaim
  namespace: mumu
spec:
  storageClassName: "managed-nfs-storage"
  accessModes:
    - ReadWriteMany
  resources: 
    requests:
      storage: 1Gi

